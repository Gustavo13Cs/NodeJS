<h1>Lista de Posts: </h1>

{{#each posts}}

    <small>{{createdAt}}</small>
    <h1>{{titulo}}</h1>
    <p>{{conteudo}}</p>
    <a href="/deletar/{{id}}"></a><button>Deletar</button></a>
    <hr>
{{/each}}

